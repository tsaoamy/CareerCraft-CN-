FROM node:20-alpine

WORKDIR /app

ENV NODE_ENV=production
ENV NEXT_TELEMETRY_DISABLED=1
ENV PORT=3000

COPY . .

RUN mkdir -p /data && chown node:node /data

EXPOSE 3000
USER node
CMD ["node", "server.js"]
